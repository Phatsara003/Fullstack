<% include ../partials/header %>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Price</th>
            <td><a  class="btn btn-primary" href="/products/<%=product.id%>" role="button">Add new</a></td>
        </tr>
         
    </thead>
    <tbody>
    <%products.forEach(function(product){ %>
<tr>
            <td scope="row"><%= product.id %></td>
            <td><%= product.title %></td>
            <td><%= product.price %></td>
            <td><a  class="btn btn-danger" href="/products/<%=product.id%>" role="button">Edit</a>
            <a  class="btn btn-warning" href="#" role="button">Update</a></td>
        </tr>
        
    <% }); %>
        
        
    </tbody>

    
</table>

 